# Fallback if Dokploy still uses Nixpacks instead of Dockerfile
[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[start]
cmd = "node scripts/prod-start.js"

[variables]
PORT = "3000"
HOSTNAME = "0.0.0.0"
