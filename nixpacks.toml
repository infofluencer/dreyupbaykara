# Dokploy / Nixpacks: Next.js 16 requires Node >= 20.9
# Production app listens on 3005 — set Dokploy domain/container port to 3005
[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[start]
cmd = "npx next start --hostname 0.0.0.0 --port 3005"

[variables]
PORT = "3005"
